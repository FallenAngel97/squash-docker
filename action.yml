name: 'docker-squash-action'
description: 'Execute docker squash python script in Github action'
author: 'FallenAngel97'
inputs:
  milliseconds: # change this
    required: true
    description: 'input description here'
    default: 'default value if applicable'
runs:
  using: 'node16'
  main: 'dist/index.js'
